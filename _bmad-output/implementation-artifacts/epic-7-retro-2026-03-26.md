# Epic 7 回顾纪要

**项目：** xtask_todo  
**Epic：** 7 — 追溯、测试覆盖率与产品边界（FR32–FR34）  
**日期：** 2026-03-26  
**参与者（ facilitated 角色）：** Bob（Scrum Master）、Alice（Product Owner）、Charlie（Senior Dev）、Dana（QA）、Richard（Project Lead）

---

## 1. Epic 摘要

| 指标 | 值 |
|------|-----|
| 故事总数 | 3（7.1 / 7.2 / 7.3） |
| 完成状态 | 3/3 `done`（`sprint-status.yaml`） |
| 性质 | **以文档、工具链对齐与对外边界澄清为主**；非大规模功能 Epic |

**Epic 标题（`epics.md`）：** 维护者能在文档体系中追溯需求—设计—用例—验收 ID；运行测试与覆盖率工作流；系统不默认承诺 HTTP API、多租户、自动迁移等未列入 PRD 的能力。

---

## 2. 故事层洞察（来自实现故事文件）

### 共同模式

- **单一事实来源（SoT）反复出现：** `coverage.rs` 与 `test-coverage.md`（7.2）、`requirements §2` / `prd.md` FR34（7.3）、`docs/` 主线与 `epics.md` FR 抽样（7.1）。
- **棕地策略一致：** 以审计、补链、最小文案修正为主，避免在文档 Epic 中夹带未立项功能。
- **验证命令明确：** `cargo xtask acceptance --stdout-only`、`cargo test -p xtask`、`cargo xtask coverage`（环境允许时）在 Completion Notes 中有记录。

### 各故事要点

- **7.1 追溯：** 完成 requirements → test-cases → acceptance 走查；FR 抽样 N=6；SKIP/手工用语核对为 no-op；File List 经审查补全。
- **7.2 覆盖率：** 明确 **95% 为团队目标**，实现**仅打印摘要、不在代码中硬失败**；repl/`beta-vm` 等排除与 `test-cases` VM 引用对齐。
- **7.3 产品边界：** 根目录与 crate `README` 增加 **Product boundary (FR34)**；关键词审计与代码抽查（侧车等为可选路径）。

### 技术债 / 待观察（轻量）

- **覆盖率硬门禁：** 若未来 CI 要求 **低于 95% 即失败**，需在 `cmd_coverage` 与文档中**显式**引入，避免静默改变行为（故事 AC 已提示）。
- **CHANGELOG vs Release（NFR-R2）：** `publishing.md` 已约定以 GitHub Release 为 SoT；若引入根目录 `CHANGELOG.md`，需显式切换规则以免双轨。

---

## 3. 上一 Epic 回顾的连续性

- **未找到** `_bmad-output/implementation-artifacts/epic-6-retro-*.md`。  
- **本次视为「首次在仓库中落盘」的 Epic 级回顾**；后续 Epic 可对比本文件中的行动项是否落实。

---

## 4. 下一 Epic / 路线图

- **`epics.md` 中 Epic 清单止于 Epic 7**；**尚无 Epic 8 定义**。  
- **下一工作**更多是：**新范围立项**（PRD/epics 更新）→ **`bmad-sprint-planning`** → **Create Story**，而非自动延续编号 Epic。

---

## 5. 团队对话摘要（Party Mode 风格纪要）

**Bob (Scrum Master):** 「今天我们回顾 Epic 7：三件故事都 marked done，属于『文档与契约收口』型交付。」

**Alice (Product Owner):** 「FR32–FR34 把『维护者能追溯』『覆盖率工作流诚实』『对外不夸大承诺』说清楚了，和 PRD 边界一致。」

**Charlie (Senior Dev):** 「7.2 把 `coverage.rs` 当 SoT 与 `test-coverage.md` 对齐，避免文档与实现各说各话——后面改排除列表时要继续双改。」

**Dana (QA Engineer):** 「验收与用例链在 7.1 走通后，自动化与人工 SKIP 的界限更清晰；7.3 减少 README 被误读成托管服务的风险。」

**Richard (Project Lead):** 「[在正式仪式中由负责人补充：最满意的一点 / 最大风险]」*— 可在下方行动项执行时补一句即可。*

---

## 6. 行动项（SMART）

| # | 行动 | 负责人 | 成功标准 | 类别 |
|---|------|--------|----------|------|
| A1 | 若下一版本发布，在 **GitHub Release** 中写清变更说明（与 `publishing.md` NFR-R2 一致） | 维护者 | Release 正文可追溯破坏性变更与用户影响 | 流程 / 发布 |
| A2 | 若 CI 将来要 **强制覆盖率阈值**，在 `xtask/src/coverage.rs` 与 `docs/test-coverage.md` **同一次 PR** 中落地 | Dev | 行为与文档一致、无静默失败 | 技术 |
| A3 | 新 Epic 或大范围需求前，**先**更新 `epics.md` / PRD，再跑 **sprint-planning** | SM / Richard | 新的 `sprint-status.yaml` 与范围一致 | 规划 |

---

## 7. Epic 7「就绪」结论（文档型 Epic）

- **测试：** 故事 7.1/7.2 已记录 `cargo xtask acceptance`、`cargo test` 等验证；7.3 以 Markdown 与可选 `xtask` 测试为主。  
- **交付物：** 主仓库文档与 README 已更新；**非**独立二进制发布 Epic。  
- **干系人：** 维护者与 crates.io 读者；**无**单独「业务方签收」门槛，除非团队自行定义。

**结论：** 从 **story 完成 + 文档一致性** 角度，Epic 7 **可视为完成**；剩余是**可选**的持续治理（发布说明、CI 策略）。

---

## 8. 下一步（给 Richard）

1. 审阅本文件，按需补充「负责人最终一句话」。  
2. 若认同行动项，在 issue 或团队习惯处跟踪 A1–A3。  
3. 有新范围时：更新规划产物 → `bmad-sprint-planning` → 新故事循环。

---

*本文件由 `bmad-retrospective` 工作流生成，基于 `_bmad-output/implementation-artifacts/7-*.md` 与 `sprint-status.yaml`。*
