[package]
name = "dev_shell"
version = "0.1.0"
edition = "2021"

[dependencies]
rustyline = "17"
xtask-todo-lib = { path = "../../crates/todo" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
# 暂无
